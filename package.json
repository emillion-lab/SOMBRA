{
  "name": "sombra",
  "version": "1.0.0",
  "private": true,
  "description": "Одитор на твърдения: двупосочна проверка, регистър в D1, измерване на калибрацията",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "cp src/index.js /tmp/check.mjs && node --check /tmp/check.mjs",
    "calibrate": "node scripts/run-calibration.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
